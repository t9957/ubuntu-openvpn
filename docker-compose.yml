services:
  openvpn:
    image: ubuntu-openvpn
    build: .
    container_name: ubuntu-openvpn
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun
    ports:
      - "${VPN_PORT}:1194/udp"
    volumes:
      - ./clients:/etc/openvpn/clients/
      - ./sh:/etc/openvpn/sh/
      - ./server.conf:/etc/openvpn/server.conf
    environment:
      - TZ=Asia/Tokyo
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "test", "-f", "/etc/openvpn/pki/ca.crt" ]
      interval: 30s
      timeout: 5s
      retries: 2
      start_period: 60s # 起動直後はここで指定した時間だけ失敗を無視
